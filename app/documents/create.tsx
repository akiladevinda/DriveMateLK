import { FeatureStubScreen } from '@/components/shared/FeatureStubScreen';

export default function CreateDocumentScreen() {
  return (
    <FeatureStubScreen
      description="Upload a vehicle document with metadata. File picker integration will attach storage paths."
      title="Upload document"
    />
  );
}
