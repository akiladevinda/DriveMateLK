import { FeatureStubScreen } from '@/components/shared/FeatureStubScreen';

export default function Screen() {
  return (
    <FeatureStubScreen
      description="Book service — functional shell ready for data integration."
      title="Book service"
    />
  );
}
