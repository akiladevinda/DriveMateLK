import { FeatureStubScreen } from '@/components/shared/FeatureStubScreen';

export default function Screen() {
  return (
    <FeatureStubScreen
      description="Maintenance detail — functional shell ready for data integration."
      title="Maintenance detail"
    />
  );
}
