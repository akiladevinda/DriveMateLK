import { FeatureStubScreen } from '@/components/shared/FeatureStubScreen';

export default function Screen() {
  return (
    <FeatureStubScreen
      description="Service detail — functional shell ready for data integration."
      title="Service detail"
    />
  );
}
